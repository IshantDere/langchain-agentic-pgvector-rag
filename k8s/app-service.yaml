apiVersion: v1
kind: Service
metadata:
  name: langchain-service
  namespace: rag-app
spec:
  type: NodePort
  selector:
    app: langchain-app
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30081